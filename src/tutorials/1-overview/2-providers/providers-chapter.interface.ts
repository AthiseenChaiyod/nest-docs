// เหมือนตอนที่เราสร้าง Dto นั่นแหละ
// แค่เปลี่ยน class เป็น interface และใช้ ; ปิดท้ายแทนที่จะเป็น ,
export interface ProvidersChapterInterface {
  name: string;
  age: number;
  gender: 'male' | 'female';
}
